diff --git a/src/Cloudflare/Workers/Vite.ts b/src/Cloudflare/Workers/Vite.ts
--- a/src/Cloudflare/Workers/Vite.ts
+++ b/src/Cloudflare/Workers/Vite.ts
@@ -4,9 +4,42 @@
 import * as Effect from "effect/Effect";
 import * as Redacted from "effect/Redacted";
 import { createRequire } from "node:module";
+import * as NodeNet from "node:net";
 import path from "node:path";
 import { pathToFileURL } from "node:url";
 import type * as vite from "vite";
+
+const reservedVitePorts = new Set<number>();
+const viteServerPorts = new WeakMap<vite.ViteDevServer, number>();
+
+const reserveAvailableVitePort = async (): Promise<number> => {
+  while (true) {
+    const port = await new Promise<number>((resolve, reject) => {
+      const server = NodeNet.createServer();
+      server.once("error", reject);
+      server.listen(
+        { host: "127.0.0.1", port: 0, exclusive: true },
+        () => {
+          const address = server.address();
+          if (!address || typeof address === "string") {
+            server.close(() =>
+              reject(new Error("Failed to allocate a Vite development port")),
+            );
+            return;
+          }
+          server.close((error) =>
+            error ? reject(error) : resolve(address.port),
+          );
+        },
+      );
+      server.unref();
+    });
+    if (!reservedVitePorts.has(port)) {
+      reservedVitePorts.add(port);
+      return port;
+    }
+  }
+};
 
 export const viteDev = (
   rootDir: string = process.cwd(),
@@ -16,19 +49,48 @@
 ) =>
   Effect.acquireRelease(
     Effect.promise(async () => {
-      const vite = await loadVite(rootDir);
-      const devServer = await vite.createServer({
-        root: rootDir,
-        define: getDefine(env),
-        plugins: [cloudflare(pluginOptions)],
-        server: serverOptions,
-      });
-      await devServer.listen();
-      return devServer;
+      const reservedPort =
+        serverOptions.port === 0
+          ? await reserveAvailableVitePort()
+          : undefined;
+      try {
+        const vite = await loadVite(rootDir);
+        const devServer = await vite.createServer({
+          root: rootDir,
+          define: getDefine(env),
+          plugins: [cloudflare(pluginOptions)],
+          server:
+            reservedPort === undefined
+              ? serverOptions
+              : {
+                  ...serverOptions,
+                  host: "127.0.0.1",
+                  port: reservedPort,
+                  strictPort: true,
+                },
+        });
+        await devServer.listen();
+        if (reservedPort !== undefined) {
+          viteServerPorts.set(devServer, reservedPort);
+        }
+        return devServer;
+      } catch (error) {
+        if (reservedPort !== undefined) {
+          reservedVitePorts.delete(reservedPort);
+        }
+        throw error;
+      }
     }),
     (devServer) =>
       Effect.promise(async () => {
-        await devServer.close();
+        const reservedPort = viteServerPorts.get(devServer);
+        try {
+          await devServer.close();
+        } finally {
+          if (reservedPort !== undefined) {
+            reservedVitePorts.delete(reservedPort);
+          }
+        }
       }),
   );
 
diff --git a/src/Bundle/Bundle.ts b/src/Bundle/Bundle.ts
--- a/src/Bundle/Bundle.ts
+++ b/src/Bundle/Bundle.ts
@@ -264,21 +264,28 @@
   bundle: rolldown.OutputBundle,
 ): Effect.Effect<BundleOutput, BundleError> {
   const files = Object.values(bundle);
-  // These are sanity checks - with rolldown, the first file is always an entry chunk.
-  if (!files[0] || files[0].type !== "chunk" || !files[0].isEntry) {
+  const entry = files.find(
+    (file): file is rolldown.OutputChunk =>
+      file.type === "chunk" && file.isEntry,
+  );
+  if (entry === undefined) {
     return Effect.fail(
       new BundleError({
-        message: "Invalid bundle output",
+        message: "Invalid bundle output: no entry chunk found",
       }),
     );
   }
-  return Effect.forEach(
-    files as [
-      rolldown.OutputChunk,
-      ...(rolldown.OutputChunk | rolldown.OutputAsset)[],
-    ],
-    bundleFileFromOutputChunk,
-  ).pipe(Effect.flatMap(bundleOutputFromFiles));
+  return Effect.all({
+    entry: bundleFileFromOutputChunk(entry),
+    remaining: Effect.forEach(
+      files.filter((file) => file !== entry),
+      bundleFileFromOutputChunk,
+    ),
+  }).pipe(
+    Effect.flatMap(({ entry, remaining }) =>
+      bundleOutputFromFiles([entry, ...remaining]),
+    ),
+  );
 }
 
 /**
diff --git a/src/Cloudflare/Workers/Worker.ts b/src/Cloudflare/Workers/Worker.ts
index 356e26c5423039ebde64ab0e1b32e2f4e21a58ec..245ce3e1b4a4439318580ffaf03ccac87b583a89 100644
--- a/src/Cloudflare/Workers/Worker.ts
+++ b/src/Cloudflare/Workers/Worker.ts
@@ -775,6 +775,28 @@ export const LiveWorkerProvider = () =>
           })
           .pipe(Effect.map((result) => result.subdomain));
 
+      // Cloudflare can return HTTP 200 with an empty body after deleting a
+      // domain, which the generated client reports as a response parse error.
+      // Verify live state before deciding whether the delete actually failed.
+      const deleteWorkerDomain = (accountId: string, domainId: string) =>
+        workers.deleteDomain({ accountId, domainId }).pipe(
+          Effect.matchEffect({
+            onSuccess: () => Effect.void,
+            onFailure: (deleteError) =>
+              workers.listDomains({ accountId }).pipe(
+                Effect.matchEffect({
+                  onFailure: () => Effect.fail(deleteError),
+                  onSuccess: (response) =>
+                    (response.result ?? []).some(
+                      (domain) => domain.id === domainId,
+                    )
+                      ? Effect.fail(deleteError)
+                      : Effect.void,
+                }),
+              ),
+          }),
+        );
+
       // Toggle the workers.dev subdomain via `POST /subdomain` with
       // `enabled: true | false`. Mirrors the upstream Alchemy
       // implementation in `.vendor/alchemy/.../worker-subdomain.ts`.
@@ -953,10 +972,8 @@ export const LiveWorkerProvider = () =>
           // don't try to delete domains we no longer track.
           const toRemove = liveAll.filter((d) => !desiredSet.has(d.hostname));
           yield* Effect.all(
-            toRemove.map((d) =>
-              workers
-                .deleteDomain({ accountId, domainId: d.id })
-                .pipe(Effect.catchTag("DomainNotFound", () => Effect.void)),
+            toRemove.map((domain) =>
+              deleteWorkerDomain(accountId, domain.id),
             ),
             { concurrency: "unbounded" },
           );
@@ -2187,16 +2204,7 @@ export const LiveWorkerProvider = () =>
             yield* Effect.all(
               liveDomains.flatMap((d) =>
                 d.id
-                  ? [
-                      workers
-                        .deleteDomain({
-                          accountId: output.accountId,
-                          domainId: d.id,
-                        })
-                        .pipe(
-                          Effect.catchTag("DomainNotFound", () => Effect.void),
-                        ),
-                    ]
+                  ? [deleteWorkerDomain(output.accountId, d.id)]
                   : [],
               ),
               { concurrency: "unbounded" },
